import { CeoCommandCenter } from "@/components/ceo-command-center";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function CeoCommandCenterPage() {
  return <CeoCommandCenter />;
}
