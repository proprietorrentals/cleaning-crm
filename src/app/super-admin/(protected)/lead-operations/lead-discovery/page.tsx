import { SuperAdminLeadDiscoveryWorkspace } from "@/components/super-admin-lead-discovery-workspace";

export const dynamic = "force-dynamic";
export const revalidate = 0;

export default function SuperAdminLeadDiscoveryPage() {
  return <SuperAdminLeadDiscoveryWorkspace />;
}
